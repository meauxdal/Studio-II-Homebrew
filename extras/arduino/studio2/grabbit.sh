#
# Pulls the most updated versions of the code into the arduino build.
# #include <directory> doesn't work.
#
cp ../../../tools/emulator/cpu.? .
cp ../../../tools/emulator/general.h .
cp ../../../tools/emulator/*1802*.h .
cp ../../../tools/emulator/system.h .
